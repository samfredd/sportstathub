import { PredictionsHub } from "@/components/predictions/PredictionsHub";

export const metadata = {
  title: "Predictions Hub | SportStatHub",
  description: "Filter multi-sport predictions by sport, market, league, and date.",
};

export default function PredictionsPage() {
  return <PredictionsHub />;
}

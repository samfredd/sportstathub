import { CodeHub } from "@/components/codes/CodeHub";

export const metadata = {
  title: "Betting Codes | SportStatHub",
  description: "Convert betting codes between bookmakers and browse community-submitted codes.",
};

export default function CodesPage() {
  return <CodeHub />;
}

/**
 * Auto-generated OpenAPI types.
 *
 * LOCAL DEV — regenerate after starting the backend:
 *   cd frontend && npm run codegen
 *
 * CI/CD — generated automatically from /openapi.json during the backend CI job.
 *
 * Do not edit manually.
 */

// Placeholder until `npm run codegen` is run against the live backend.
// CI overwrites this file on every build with types derived from the real spec.
export type paths = Record<string, unknown>;
export type components = { schemas: Record<string, unknown> };
